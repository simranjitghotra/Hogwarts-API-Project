# Hogwarts-API-Project
This is a simple web page that displays Harry Potter Characters. It uses the HP API to fetch data about the witches and wizards and renders their names, houses, and photos (if available) in a list format. Currently, it is set to display only the first 10 characters.
# 🪄 Harry Potter Characters Viewer

This is a simple web application that displays a list of Harry Potter characters fetched from the [HP API](https://hp-api.onrender.com/).

## ✨ Features

* **API Integration:** Fetches character data from the HP API.
* **Dynamic Content:** Uses JavaScript to dynamically load and render characters onto the page.
* **Display:** Shows the character's name, house, and image (if available).
* **Placeholder Image:** Displays a default "No Image" placeholder if an official image is unavailable.
* **Limited View:** Currently set to display only the first 10 characters from the API response.

## ⚙️ Tech Stack

* **HTML5:** For the structure of the web page.
* **CSS3:** For styling (assuming a `style.css` file exists).
* **JavaScript (ES6+):** For fetching data from the API and DOM manipulation.

## 🚀 Installation and Running

No special setup is required to run this project.

1.  **Download Files:**
    * `index.html`
    * `style.css` (if it exists)
2.  **Open in Browser:** Double-click the `index.html` file to open it in your web browser.
3.  **View Results:** The page will load and display the characters from the HP API.

<img width="1847" height="883" alt="image" src="https://github.com/user-attachments/assets/a2732c8b-a82f-467e-896a-432f9bb95585" />
